import SwiftUI

struct addNewItem: View {
    var body: some View {
       Text("sono nella nuova pagina")
    }
}

struct addNewItem_Previews: PreviewProvider {
    static var previews: some View {
        addNewItem()
    }
}
